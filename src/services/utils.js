const BASE_URL = "http://116.63.39.40/api";

/**
 * 
 * @param {string} title 
 * @param {string} content 
 * @param {string} type 
 */
function showHint(title, content, type) {
	console.log('title: [' + title + '] content: [' + content + '] type: [' + type + ']');
}